import "./Bars.css";

const Bars = () => {
  return (
    <button className="hamburger" aria-label="Menu">
      <span className="hamburger-line" />
      <span className="hamburger-line" />
      <span className="hamburger-line" />
    </button>
  );
};

export default Bars;
